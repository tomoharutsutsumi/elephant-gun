model: train/models/eg-miniLM-finetuned
embed_dim: 384
targets:
  - table: tickets
    key: id
    time_column: created_at
    text_template: "{{title}} {{body}}"
    filters: []  # e.g., ["deleted_at IS NULL"]
